#include "clients.h"
#include <stdio.h>

int main(){
	//dynamic_array *arr_ptr;
	//initArray(&arr_ptr);
	//
	//client client_;
	//client_.test = 10;
	//addClient(arr_ptr, client_);
	//
	//printf("%zd\n", arr_ptr->size);

	//client_.test = 20;
	//addClient(arr_ptr, client_);
	//printf("%zd\n", arr_ptr->size);

	//client_ = getClient(arr_ptr, 0);
	//printf("%d\n", client_.test);

	//client_ = getClient(arr_ptr, 1);
	//printf("%d\n", client_.test);

	//deleteClient(arr_ptr, 1);
	//client_ = getClient(arr_ptr, 1);
	//
	//printf("%zd\n", arr_ptr->size);

	//freeArray(arr_ptr);
}
